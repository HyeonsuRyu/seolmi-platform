# OpenAI API 키 (CS 챗봇 LLM 호출용). 실제 키는 .env 에만 넣고 절대 커밋하지 않는다.
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 앱 실행 환경 (dev / staging / prod)
APP_ENV=dev

# 아래 두 값은 폴더 구조상 자리만 잡아둔 것 — DB·인증은 아직 구현되지 않았다 (README 현재 단계 참고)
DATABASE_URL=sqlite:///./seolmi.db
JWT_SECRET=change-me
